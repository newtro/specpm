{
  "name": "cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "specpm": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts && chmod +x dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "commander": "^13.0.0",
    "shared": "workspace:*",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
