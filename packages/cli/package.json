{
  "name": "cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "specpm": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts && chmod +x dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsup": "^8.0.0"
  }
}
